
const KEY = {
  ARROW_DOWN: "ArrowDown",
  ARROW_UP: "ArrowUp",
  ENTER: "Enter",
}

const THEME = {
  DARK: "dark",
  LIGHT: "light",
};

const CONSTANTS = {
  KEY,
  THEME,
};

export default CONSTANTS;
